# Connection Flow - Visual Guide

## 🔄 How Data Flows Through the System

### Example 1: Loading Rooms on Dashboard

```
┌─────────────────────────────────────────────────────────────────┐
│ STEP 1: User Opens Dashboard                                    │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ STEP 2: React Component Mounts                                  │
│                                                                  │
│ Dashboard.jsx:                                                   │
│   useEffect(() => {                                              │
│     API.get('/rooms')  ← Triggers HTTP request                  │
│   }, []);                                                        │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ STEP 3: Axios Sends HTTP Request                                │
│                                                                  │
│ Request Details:                                                 │
│   Method: GET                                                    │
│   URL: http://localhost:5000/api/rooms                          │
│   Headers: { Content-Type: 'application/json' }                 │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ STEP 4: Express Server Receives Request                         │
│                                                                  │
│ server.js:                                                       │
│   app.use('/api/rooms', roomsRouter);  ← Routes to roomRoutes   │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ STEP 5: Route Handler Executes                                  │
│                                                                  │
│ roomRoutes.js:                                                   │
│   router.get('/', async (req, res) => {                         │
│     const [rows] = await pool.query(                            │
│       'SELECT * FROM Room ORDER BY Room_No'                     │
│     );                                                           │
│   });                                                            │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ STEP 6: Connection Pool Gets Connection                         │
│                                                                  │
│ db.js:                                                           │
│   pool.query() → Gets available connection from pool            │
│                                                                  │
│ Connection Pool Status:                                          │
│   [Conn1: In Use] [Conn2: Available] [Conn3: Available] ...    │
│         ↑                                                        │
│      Uses this                                                   │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ STEP 7: MySQL Executes Query                                    │
│                                                                  │
│ SQL Query:                                                       │
│   SELECT * FROM Room ORDER BY Room_No;                          │
│                                                                  │
│ MySQL Process:                                                   │
│   1. Parse SQL                                                   │
│   2. Check indexes                                               │
│   3. Scan Room table                                             │
│   4. Sort by Room_No                                             │
│   5. Return result set                                           │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ STEP 8: MySQL Returns Data                                      │
│                                                                  │
│ Result Set:                                                      │
│   [                                                              │
│     { Room_No: 101, Category: 'Single', Rent: 1500, ... },     │
│     { Room_No: 102, Category: 'Single', Rent: 1500, ... },     │
│     { Room_No: 103, Category: 'Double', Rent: 2500, ... },     │
│     ...                                                          │
│   ]                                                              │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ STEP 9: Backend Sends JSON Response                             │
│                                                                  │
│ roomRoutes.js:                                                   │
│   res.json(rows);  ← Sends data back to frontend                │
│                                                                  │
│ HTTP Response:                                                   │
│   Status: 200 OK                                                 │
│   Content-Type: application/json                                │
│   Body: [{ Room_No: 101, ... }, ...]                           │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ STEP 10: Axios Receives Response                                │
│                                                                  │
│ Dashboard.jsx:                                                   │
│   API.get('/rooms')                                              │
│     .then(response => {                                          │
│       setRooms(response.data);  ← Updates React state           │
│     });                                                          │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ STEP 11: React Re-renders Component                             │
│                                                                  │
│ State Update:                                                    │
│   rooms = [{ Room_No: 101, ... }, ...]                         │
│                                                                  │
│ UI Updates:                                                      │
│   {rooms.map(room => <div>{room.Room_No}</div>)}               │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ STEP 12: User Sees Data on Screen                               │
│                                                                  │
│ Browser Display:                                                 │
│   ┌──────────────────────────────────────────┐                 │
│   │ Room 101 - Single - ₹1500 - Available   │                 │
│   │ Room 102 - Single - ₹1500 - Available   │                 │
│   │ Room 103 - Double - ₹2500 - Occupied    │                 │
│   └──────────────────────────────────────────┘                 │
└─────────────────────────────────────────────────────────────────┘
```

**Total Time: ~50-200ms** ⚡

---

## 📝 Example 2: Creating a New Guest

```
┌─────────────────────────────────────────────────────────────────┐
│ STEP 1: User Fills Form                                         │
│                                                                  │
│ Form Fields:                                                     │
│   Name: "John Doe"                                               │
│   Email: "john@example.com"                                      │
│   Contact: "9876543210"                                          │
│   Nationality: "India"                                           │
│   Gender: "Male"                                                 │
│                                                                  │
│ User clicks: [Add Guest] button                                 │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ STEP 2: React Calls API                                         │
│                                                                  │
│ Guests.jsx:                                                      │
│   const add = async () => {                                      │
│     await API.post('/guests', {                                  │
│       Name: 'John Doe',                                          │
│       Email: 'john@example.com',                                 │
│       Contact_Info: '9876543210',                                │
│       Nationality: 'India',                                      │
│       Gender: 'Male'                                             │
│     });                                                          │
│   };                                                             │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ STEP 3: HTTP POST Request                                       │
│                                                                  │
│ Request:                                                         │
│   POST http://localhost:5000/api/guests                         │
│   Content-Type: application/json                                │
│                                                                  │
│   Body:                                                          │
│   {                                                              │
│     "Name": "John Doe",                                          │
│     "Email": "john@example.com",                                 │
│     "Contact_Info": "9876543210",                                │
│     "Nationality": "India",                                      │
│     "Gender": "Male"                                             │
│   }                                                              │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ STEP 4: Express Parses Request                                  │
│                                                                  │
│ server.js:                                                       │
│   app.use(express.json());  ← Parses JSON body                  │
│   app.use('/api/guests', guestsRouter);                         │
│                                                                  │
│ req.body now contains:                                           │
│   { Name: 'John Doe', Email: 'john@example.com', ... }         │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ STEP 5: Route Handler Extracts Data                             │
│                                                                  │
│ guestRoutes.js:                                                  │
│   router.post('/', async (req, res) => {                        │
│     const { Name, Contact_Info, Email,                          │
│             Nationality, Gender } = req.body;                    │
│                                                                  │
│     // Prepare SQL query                                         │
│     const query = 'INSERT INTO Guest (...) VALUES (?, ?, ...)'; │
│     const values = [Name, Contact_Info, Email, ...];            │
│   });                                                            │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ STEP 6: Execute INSERT Query                                    │
│                                                                  │
│ SQL Query (Parameterized):                                       │
│   INSERT INTO Guest                                              │
│     (Name, Contact_Info, Email, Nationality, Gender)            │
│   VALUES (?, ?, ?, ?, ?)                                        │
│                                                                  │
│ Parameters:                                                      │
│   ['John Doe', '9876543210', 'john@example.com',               │
│    'India', 'Male']                                             │
│                                                                  │
│ MySQL escapes and executes:                                      │
│   INSERT INTO Guest VALUES                                       │
│     ('John Doe', '9876543210', 'john@example.com', ...)        │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ STEP 7: MySQL Inserts Record                                    │
│                                                                  │
│ Before:                                                          │
│   Guest Table:                                                   │
│   ┌──────────┬──────────┬─────────────────┐                    │
│   │ Guest_ID │   Name   │      Email      │                    │
│   ├──────────┼──────────┼─────────────────┤                    │
│   │    1     │ Alice    │ alice@email.com │                    │
│   │    2     │ Bob      │ bob@email.com   │                    │
│   └──────────┴──────────┴─────────────────┘                    │
│                                                                  │
│ After:                                                           │
│   Guest Table:                                                   │
│   ┌──────────┬──────────┬─────────────────────┐                │
│   │ Guest_ID │   Name   │        Email        │                │
│   ├──────────┼──────────┼─────────────────────┤                │
│   │    1     │ Alice    │ alice@email.com     │                │
│   │    2     │ Bob      │ bob@email.com       │                │
│   │    3     │ John Doe │ john@example.com    │ ← NEW!         │
│   └──────────┴──────────┴─────────────────────┘                │
│                                                                  │
│ Returns: { insertId: 3, affectedRows: 1 }                      │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ STEP 8: Backend Sends Success Response                          │
│                                                                  │
│ guestRoutes.js:                                                  │
│   res.json({ insertedId: result.insertId });                    │
│                                                                  │
│ HTTP Response:                                                   │
│   Status: 200 OK                                                 │
│   Body: { "insertedId": 3 }                                     │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ STEP 9: Frontend Reloads Guest List                             │
│                                                                  │
│ Guests.jsx:                                                      │
│   await API.post('/guests', form);  ← Success!                  │
│   const res = await API.get('/guests');  ← Reload list          │
│   setGuests(res.data);  ← Update state                          │
│   showMessage('success', 'Guest added');  ← Show notification   │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ STEP 10: UI Updates                                             │
│                                                                  │
│ Browser Display:                                                 │
│   ┌────────────────────────────────────────────┐               │
│   │ ✅ Guest added successfully                │               │
│   ├────────────────────────────────────────────┤               │
│   │ ID │ Name     │ Email              │       │               │
│   ├────┼──────────┼────────────────────┤       │               │
│   │ 1  │ Alice    │ alice@email.com    │       │               │
│   │ 2  │ Bob      │ bob@email.com      │       │               │
│   │ 3  │ John Doe │ john@example.com   │ ← NEW!│               │
│   └────┴──────────┴────────────────────┴───────┘               │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Example 3: Booking a Room (Complex Flow)

```
┌─────────────────────────────────────────────────────────────────┐
│ STEP 1: Customer Selects Room                                   │
│                                                                  │
│ CustomerPortal.jsx:                                              │
│   - User selects dates: Jan 15 - Jan 20 (5 nights)             │
│   - User clicks "Book Now" on Room 101                          │
│   - handleBookRoom(101) is called                                │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ STEP 2: Create/Find Guest                                       │
│                                                                  │
│ First, ensure guest exists:                                      │
│   POST /api/guests                                               │
│   { Name: 'Jane', Email: 'jane@email.com', ... }               │
│                                                                  │
│ Then get guest ID:                                               │
│   GET /api/guests                                                │
│   Find guest where Email = 'jane@email.com'                     │
│   → Guest_ID = 5                                                 │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ STEP 3: Create Reservation                                      │
│                                                                  │
│ POST /api/reservations                                           │
│ {                                                                │
│   guestId: 5,                                                    │
│   roomNo: 101,                                                   │
│   checkIn: '2025-01-15',                                        │
│   checkOut: '2025-01-20'                                        │
│ }                                                                │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ STEP 4: Backend Calls Stored Procedure                          │
│                                                                  │
│ reservationRoutes.js:                                            │
│   await conn.query('CALL BookRoom(?, ?, ?, ?)',                │
│     [5, 101, '2025-01-15', '2025-01-20']);                     │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ STEP 5: MySQL Stored Procedure Executes                         │
│                                                                  │
│ BookRoom Procedure:                                              │
│                                                                  │
│ 1. Validate dates:                                               │
│    IF checkOut <= checkIn THEN                                   │
│      SIGNAL ERROR 'Invalid dates'                                │
│                                                                  │
│ 2. Check guest exists:                                           │
│    SELECT * FROM Guest WHERE Guest_ID = 5                       │
│    IF NOT FOUND THEN ERROR                                       │
│                                                                  │
│ 3. Check room availability:                                      │
│    SELECT Status FROM Room WHERE Room_No = 101                  │
│    IF Status != 'Available' THEN ERROR                          │
│                                                                  │
│ 4. Check for conflicts:                                          │
│    SELECT * FROM Reservation                                     │
│    WHERE Room_No = 101                                           │
│      AND Status IN ('Confirmed', 'CheckedIn')                   │
│      AND dates overlap                                           │
│    IF FOUND THEN ERROR 'Room already booked'                    │
│                                                                  │
│ 5. Calculate total:                                              │
│    SELECT Rent FROM Room WHERE Room_No = 101                    │
│    → Rent = 1500                                                 │
│    days = DATEDIFF('2025-01-20', '2025-01-15') = 5             │
│    total = 1500 × 5 = 7500                                      │
│                                                                  │
│ 6. Create reservation:                                           │
│    INSERT INTO Reservation                                       │
│      (Guest_ID, Room_No, Check_In_Date,                         │
│       Check_Out_Date, Total_Amount, Status)                     │
│    VALUES (5, 101, '2025-01-15',                                │
│            '2025-01-20', 7500, 'Confirmed')                     │
│    → Reservation_ID = 42                                         │
│                                                                  │
│ 7. Update room status:                                           │
│    UPDATE Room SET Status = 'Occupied'                          │
│    WHERE Room_No = 101                                           │
│                                                                  │
│ 8. Update guest status:                                          │
│    UPDATE Guest SET Check_In_Status = TRUE                      │
│    WHERE Guest_ID = 5                                            │
│                                                                  │
│ 9. Return success                                                │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ STEP 6: Database State After Booking                            │
│                                                                  │
│ Reservation Table:                                               │
│   ┌────┬─────────┬────────┬──────────┬───────────┬────────┐   │
│   │ ID │ Guest_ID│ Room_No│ Check_In │ Check_Out │ Amount │   │
│   ├────┼─────────┼────────┼──────────┼───────────┼────────┤   │
│   │ 42 │    5    │  101   │ 01-15    │  01-20    │  7500  │   │
│   └────┴─────────┴────────┴──────────┴───────────┴────────┘   │
│                                                                  │
│ Room Table:                                                      │
│   ┌─────────┬──────────┬──────┬──────────┐                     │
│   │ Room_No │ Category │ Rent │  Status  │                     │
│   ├─────────┼──────────┼──────┼──────────┤                     │
│   │   101   │  Single  │ 1500 │ Occupied │ ← Changed!          │
│   └─────────┴──────────┴──────┴──────────┘                     │
│                                                                  │
│ Guest Table:                                                     │
│   ┌──────────┬──────┬─────────────────┐                        │
│   │ Guest_ID │ Name │ Check_In_Status │                        │
│   ├──────────┼──────┼─────────────────┤                        │
│   │    5     │ Jane │      TRUE       │ ← Changed!             │
│   └──────────┴──────┴─────────────────┘                        │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ STEP 7: Success Response                                        │
│                                                                  │
│ Backend → Frontend:                                              │
│   Status: 200 OK                                                 │
│   Body: { message: 'Reservation created' }                      │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ STEP 8: Frontend Updates                                        │
│                                                                  │
│ CustomerPortal.jsx:                                              │
│   showMessage('success', 'Room booked successfully!');          │
│   loadRooms();  ← Reload available rooms                        │
│   loadMyBookings();  ← Reload booking history                   │
│                                                                  │
│ UI Shows:                                                        │
│   ✅ Room booked successfully!                                  │
│   Room 101 no longer appears in available rooms                 │
│   Booking #42 appears in "My Bookings"                          │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔐 Security: How Parameterized Queries Work

### ❌ Vulnerable Code (SQL Injection Risk)

```javascript
// DON'T DO THIS!
const name = req.body.name;  // User input: "John'; DROP TABLE Guest; --"
const query = `INSERT INTO Guest (Name) VALUES ('${name}')`;
await pool.query(query);

// Executed SQL:
// INSERT INTO Guest (Name) VALUES ('John'); DROP TABLE Guest; --')
//                                          ↑ Malicious code executed!
```

### ✅ Safe Code (Parameterized Query)

```javascript
// DO THIS!
const name = req.body.name;  // User input: "John'; DROP TABLE Guest; --"
const query = 'INSERT INTO Guest (Name) VALUES (?)';
await pool.query(query, [name]);

// MySQL automatically escapes:
// INSERT INTO Guest (Name) VALUES ('John\'; DROP TABLE Guest; --')
//                                       ↑ Treated as literal text, not code
```

---

## 📊 Performance: Connection Pooling

### Without Connection Pool (Slow)

```
Request 1: Create connection → Query → Close connection (500ms)
Request 2: Create connection → Query → Close connection (500ms)
Request 3: Create connection → Query → Close connection (500ms)
Total: 1500ms
```

### With Connection Pool (Fast)

```
Startup: Create 10 connections (one-time cost)

Request 1: Get Conn1 → Query → Return Conn1 (50ms)
Request 2: Get Conn2 → Query → Return Conn2 (50ms)
Request 3: Get Conn3 → Query → Return Conn3 (50ms)
Total: 150ms (10x faster!)
```

---

## 🎯 Key Takeaways

1. **Frontend (React)** makes HTTP requests using axios
2. **Backend (Express)** receives requests and routes them
3. **Database (MySQL)** stores and retrieves data
4. **Connection Pool** reuses database connections for speed
5. **Parameterized Queries** prevent SQL injection
6. **Stored Procedures** encapsulate complex business logic
7. **Triggers** automatically update related data
8. **JSON** is the data format for communication

This architecture is **secure**, **fast**, and **scalable**! 🚀
